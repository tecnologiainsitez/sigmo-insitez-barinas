import { Patient } from '../types';

/**
 * Base de datos inicial de Pacientes (Padrón UNELLEZ).
 * Inicializado limpio y preparado para registrar pacientes reales en producción.
 */
export const INITIAL_PATIENTS: Patient[] = [];
